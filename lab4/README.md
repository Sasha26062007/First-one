# Laboratory Work 4
Student: Oleksandr Panayotov, Group FB-44
Topic: Data Visualization 2 - Interactive Harmonic Functions
## Installation
1. Create and activate a virtual environment.
2. Install dependencies using the following command:
`pip install -r requirements.txt`
## Usage
Open the Jupyter Notebook and run the cells. Use the sliders to adjust amplitude, frequency, phase, and noise parameters. Toggle the noise display using the checkbox, or reset to default using the button.
